_target_: src.adapters.training_adapter.PyTorchTrainer
config:
  optimizer:
    type: AdamW
    lr: 0.001
    weight_decay: 0.01

  scheduler:
    type: CosineAnnealingLR
    T_max: 100

  loss:
    type: CrossEntropyLoss
